{
  "name": "form-guard-aws-sam",
  "private": true,
  "description": "フォーム営業撲滅AI 経路C: AWS SAM 参照実装（雛形）",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --guided"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.109.0",
    "@aws-sdk/client-dynamodb": "^3.910.0",
    "@aws-sdk/client-secrets-manager": "^3.910.0",
    "@aws-sdk/client-sqs": "^3.910.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.0"
  }
}
